services:
  app:
    image: node:20-alpine
    working_dir: /workspace
    command: sh -c "npm install && npm run dev"
    volumes:
      - ./:/workspace
    ports:
      - "5173:5173"
      - "5000:5000"